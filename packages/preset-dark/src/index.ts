import { makeTheme } from '@theme-ui/css/utils'

const heading = {
  fontFamily: 'heading',
  fontWeight: 'heading',
  lineHeight: 'heading',
}

export const dark = makeTheme({
  colors: {
    text: '#fff',
    background: '#0